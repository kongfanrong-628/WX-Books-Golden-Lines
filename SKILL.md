---
name: 微信读书金句素材库
description: 对接微信读书，自动抓取书架书籍的公开热门划线与你的个人划线，按书籍类型分类沉淀为本地金句素材库；写口播稿时主动调用，按主题语境智能插入≥3条匹配金句，提升文案深度与质感。
version: 1.0.0
---

# 微信读书金句素材库

把你在微信读书里「读到的好句子」变成可复用的**口播金句弹药库**。三件事：① 采集书架书籍的公开热门划线 + 你的个人划线；② 按书籍类型（一级分类）归档进本地知识库；③ 写文案时按主题智能插入匹配的 ≥3 条金句。

## 触发场景（何时调用）

- 用户说「加金句 / 补素材 / 用金句库 / 给我的口播稿插金句 / 提升文案质感」
- 用户正在写口播稿、公众号、短视频文案、直播稿，并希望「更有深度/更有料」
- 用户要「同步微信读书划线 / 建金句库 / 更新素材库 / 每周同步金句」
- 用户提到「微信读书金句素材库」这个 skill 名

## 主动介入（重要）

**本 skill 的核心价值是「用户写文案时不用手动点名，你主动上」。** 满足以下任一情况，应**无需用户显式 @ 或要求**，主动调用 `insert.py` 为其稿件插入匹配金句并展示插入报告：

- 用户在对话中**撰写、粘贴、迭代**任意口播稿 / 文案 / 公众号 / 短视频脚本 / 直播稿等内容；
- 用户说「帮我写/改/润色一篇关于 XX 的文案/口播」且产出正文后；
- 用户把一段成稿丢进来问「看看怎么样 / 帮我把质感拉起来」。

> ⚠️ **能力边界（必须如实告知用户）**：主动介入只在 **WorkBuddy 会话内**成立——只要用户在 WorkBuddy 里写/贴文案，你就该主动调。但**无法**跨进微信、Word、备忘录等外部编辑器自动触发（那需要 OS 级集成，超出 skill 范畴）。应对办法：让用户把稿子贴进 WorkBuddy 会话，或在 WorkBuddy 里直接起草。知识库本身是纯 Markdown，可随时被读取。

## 配套资源

- 稿件模板：`templates/口播稿模板.md`（顶部 frontmatter 写 `主题`/`分类`，匹配更准）
- 补书单：`references/recommended_books.md`（金句库缺 副业/AI/认知 三类，按它补书架再从根上消除弱匹配）
- 开源分享：`README.md` + `LICENSE`(MIT) + `config.example.json`，`config.json` 与 `knowledge_base/` 已被 `.gitignore` 排除，分享不泄露私钥与私有数据。

## 前置条件

- **API Key**：微信读书 key，写在 `config.json` 的 `api_key`，或用环境变量 `WRKB_API_KEY` 覆盖（优先级更高）。
- **运行环境**：Python 3（推荐 WorkBuddy 托管 Python），可选 `jieba`（中文分词，缺失时自动降级为二元组，不影响运行）。
- **知识库路径**：`config.json` 的 `kb_dir`（默认指向 Obsidian `02-创作素材库/微信读书金句库`）。

## 工作流（三阶段）

### 阶段一 · 采集（collect）
调 `scripts/collect.py`：
1. `/shelf/sync` 取书架全部电子书（含未读完）。
2. 对每本书并行抓 `/book/bestbookmarks`（公开热门划线，前 20）与 `/book/bookmarklist`（个人划线）。
3. 按书籍 `category` 一级分类归档，自动打关键词标签，按 `md5(文本)` 去重。
4. 写入知识库（JSON + Markdown 索引 + 元信息）。可反复运行做**增量同步**。

```bash
python scripts/collect.py
```

### 阶段二 · 沉淀（知识库）
落点见 `references/knowledge_base.md`：`golden_lines.json`（机器读）+ `金句素材库.md`（人读，按分类分组）+ `meta.json`。
- 金句分类 = 书籍一级分类（个人成长 / 经济理财 / 心理 / 哲学宗教 …）。
- `source` 区分 `popular`（公域热门）与 `personal`（你的划线）。

### 阶段三 · 调用插入（insert）
写口播稿时调 `scripts/insert.py`：
- 读稿件 + 知识库，按**关键词重叠 + 分类主题命中 + 热度**打分。
- 多样性约束（每本书≤2条），筛选与主题匹配的候选。
- 分布插入到「钩子后 / 中段 / 结尾前」等自然位置，**保证 ≥3 条**。
- 输出增强稿 + 插入报告（位置/置信度/得分/命中词）。

```bash
python scripts/insert.py --manuscript 稿件.md --out 增强稿.md --min 3 --max 5
```

## 接口与参数
见 `references/collect.md`（gateway、鉴权、三个接口字段、数量口径、处码说明）。

## 知识库结构
见 `references/knowledge_base.md`（目录、JSON schema、分类 taxonomy、维护）。

## 插入匹配算法
见 `references/insert.md`（打分公式、保底逻辑、插入位置、置信度、CLI）。

## 命令速查

| 目的 | 命令 |
|------|------|
| 同步/采集金句 | `python scripts/collect.py` |
| 给稿件插金句 | `python scripts/insert.py --manuscript 稿件.md --out 增强稿.md` |
| 自定义最少条数 | 追加 `--min 4 --max 6` |
| 指定知识库位置 | `--kb /path/to/kb_dir` |

## 注意事项（红队提醒）

1. **热门划线 ≠ 你的笔记**：`bestbookmarks` 是公域（别人划得最多的），`bookmarklist` 才是你自己的。你目前几本尚未个人划线，素材库以公域为主——质量高但别当「我的读书笔记」用。
2. **无 deepLink**：bestbookmarks 不返可跳转链接，处码只给「章节 + range」，无法一键跳 App。
3. **API Key 安全**：key 明文存于 `config.json`，建议生产环境改用 `WRKB_API_KEY` 环境变量，且勿提交到公开仓库。
4. **保底≠强匹配**：当稿件主题与知识库重合度低时，脚本会插满 3 条但标 `保底(弱匹配)`，**务必人工复核**，别硬塞不相关的金句破坏语境。
5. **金句是素材不是模板**：插入后建议改成自然口播衔接，原样照搬易显生硬。
6. **增量同步**：读完新书或新划线后，重跑 `collect.py` 即可，只追加不覆盖。
